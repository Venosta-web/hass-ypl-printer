# Targets Home Assistant Core 2026.9.2 (current stable when the scaffold was
# created). 0.13.365 is the release that pins homeassistant==2026.9.2 and
# requires Python >=3.14.
pytest-homeassistant-custom-component==0.13.365

# Requirements of Core's bluetooth and usb components (dependencies of
# bluetooth_adapters), pinned as in their Core 2026.9.2 manifests. The test
# plugin does not install component requirements.
aiousbwatcher==1.1.2
bleak==3.0.2
bleak-retry-connector==4.7.0
bluetooth-adapters==2.4.0
bluetooth-auto-recovery==1.6.4
bluetooth-data-tools==1.29.24
dbus-fast==5.0.22
habluetooth==6.26.11
serialx==1.10.0
